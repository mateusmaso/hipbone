module.exports = ->
  describe "template", ->

# module.exports = ->
#   describe "templates", ->
#     it "should initialize templates and get template from path", ->
#       app = new Hipbone.Application(templatePath: "root_path/", templates: "root_path/path/to/foo": Handlebars.compile("foo"))
#       chai.expect(app.getTemplate("path/to/foo")()).to.be.deep.equal("foo")
