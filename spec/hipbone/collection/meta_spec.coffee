module.exports = ->
  describe "meta", ->
    # it "should initialize", ->
    #   collection = new Hipbone.Collection([], meta: count: 10)
    #   chai.expect(collection.meta.get("count")).to.be.equal(10)
